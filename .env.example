# Copy to .env.local and fill values
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Storage bucket for nasheeds (frontend)
NEXT_PUBLIC_SUPABASE_BUCKET=nasheed_play
# Optional: only for server-side signed URLs or Edge Functions local dev
SUPABASE_SERVICE_ROLE_KEY=
